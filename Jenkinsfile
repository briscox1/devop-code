pipeline {
  agent any
  stages {
    stage('build stage'){
      steps {
        echo " build steps"
        sleep 10
      }
    }
    stage('build stage'){
      steps {
        echo " build steps"
        sleep 10
      }
    }
  }
}
